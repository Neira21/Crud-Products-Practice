// import { HttpException, HttpStatus } from '@nestjs/common';

// export class ProductNotFoundException extends HttpException {
//   constructor(id: number) {
//     super(`Product with ID ${id} not found`, HttpStatus.NOT_FOUND);
//   }
// }

// export class ProductAlreadyExistsException extends HttpException {
//   constructor(name: string) {
//     super(`Product with name '${name}' already exists`, HttpStatus.CONFLICT);
//   }
// }

// export class InvalidProductDataException extends HttpException {
//   constructor(message: string) {
//     super(`Invalid product data: ${message}`, HttpStatus.BAD_REQUEST);
//   }
// }
